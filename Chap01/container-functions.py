import os
import random
import datetime

def main():
    # v = os.getcwd()
    # v = list(range(25))
    now = datetime.datetime.now()
    print(now.year, now.month, now.day)
    # random.shuffle(v)
    # print(v)

if __name__ == "__main__":
    main()